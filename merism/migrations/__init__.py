"""Merism database migrations.

This package starts empty. On first dev setup, run::

    python manage.py makemigrations merism
    python manage.py migrate

`bin/setup-dev.sh` does this automatically. See `docs/ROADMAP.md` §R5
for the expected initial migration contents and pgvector index plan.
"""
